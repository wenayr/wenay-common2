# 2.20.0

- H3: add trusted `publicUrl` / `SERVICE_PUBLIC_URL` and bind `host` / `SERVICE_HOST` to leader/node
  hosts. Publish the external origin in the roster while retaining local `url` and explicit CORS.
- M1: bind asynchronous recorder/PCM callbacks and startup failures to the capture generation;
  detach callbacks and tracks on stop, discard pending/final chunks and observe owned async errors.
- A1: add optional synchronous atomic AI checkpoints, request/input recovery and owner-scoped
  receipt validation. Commit before runner entry and waiter resolution; fence on persistence failure.
- Restore active runs with an explicit recovery fact. Add server reconciliation/resume facets and
  optional `runner.recover`, preserving pending approvals/inputs without silently replaying effects.
- Ship three-gateway and AI archive recovery examples with exact library dependencies, public
  type fixtures and regressions for torn writes, process termination, permissions and late results.
- Reassess H1/H2 against existing queue, Listen and host lifecycle primitives; retain them as
  separate public-contract proposals. React R4 remains outside common2.

No new dependency, paid provider call or modification of an installed consumer package.
See [SERVICE-PUBLIC-ADDRESS](../SERVICE-PUBLIC-ADDRESS.md) and
[AI-RUN-PERSISTENCE](../AI-RUN-PERSISTENCE.md) for adoption and guarantee boundaries.
