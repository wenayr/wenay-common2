# 2.19.0

- Add SC1 v1 static `definition.resources`, authority-owned async factories and inferred
  `client.resources.open(name)` controllers with stable status Stores and generation-bound remotes.
- Reuse service identity, role state and token renewal. Preserve unchanged grants; replace on
  roles/reconnect and terminally close old-account controllers. Keep ordinary view placement separate.
- Add internal RPC scope ownership for saved paths, async admission/results, shared ordinary/dynamic
  subscriptions, flow waits and replay gates.
- Bound opening/disposal, cancel late factory results, expose safe client errors and original
  server diagnostics, and return resource cleanup completion from leader close.
- Restore resource permission subscriptions after token revoke/regrant; reuse RPC's absolute
  deadline timer for grants and resource budgets beyond the platform's single-timer limit.
- Extend safe descriptors/permissions; add a generated counter/peer example and migration guide.
- Fence retained authority node links by leadership epoch as well as roster identity, closing
  a fast demotion/promotion race exposed by the existing ownership regression during release checks.
- Verify primitives, real Socket.IO cancellation/reconnect/roles/placement, raw paths,
  authority archive/receipt restart and exact installed-package/browser type boundaries.

No mandatory dependency, installed consumer-package edit, React change or dynamic code loading.
Earlier node-switching and schema object-array fixes remain included. See
[SERVICE-RESOURCES.md](../SERVICE-RESOURCES.md) for the contract and removable adapter ownership.
