# 2.1.3

- Keep RPC path proxies compatible with React Native Hermes by using a per-client strong cache when
  `WeakRef` or `FinalizationRegistry` is unavailable, while retaining automatic weak-cache cleanup
  on runtimes that provide both APIs.
