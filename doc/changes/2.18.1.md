# 2.18.1

- Extend the public service schema DSL with recursive array item specifications, including arrays
  of objects and nested arrays, without changing existing command shapes or scalar array behavior.
- Keep exact schemaCommand input/result and descriptor client inference; optionality applies to
  fields, while array elements remain required values.
- Reuse recursive validation and JSON Schema generation for every item. Preserve indexed error
  paths, finite-number checks, required fields and rejection of additional properties.
- Add positive/negative type checks against source and installed declarations; extend the shipped
  rental schema check with recipe/checklist objects, OpenAPI items, nested failures and rejection
  before domain effects/receipts.

Use `wenay-common2/service` as before. No new dependency, consumer package patch or domain change.
