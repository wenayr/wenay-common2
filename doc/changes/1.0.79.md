# 1.0.79

- Bound dynamic Store path memory: deleted unsubscribed nodes are pruned, remote writes no longer materialize nodes, and `exposeStoreReplay` reuses raw-state push patches instead of retaining every transient order/task key. Public facades stay unchanged.
