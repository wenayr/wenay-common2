# 1.0.83

- Order synchronous and early trickled ICE candidates around asynchronous offer/answer remote descriptions so direct WebRTC promotion does not fail intermittently.
- Keep media-relay resource cleanup independent of the public watch ACL after access is revoked.
- Preserve `MediaSource.state` as a live accessor through capture permission, device, and runtime transitions.
- Add a participant-based relay media stand with visible rooms, camera, microphone, screen sharing, room playback, and separate private calls.
- Refactor the media stand into small dependency-injected example factories without changing the public library API.
- Add reproducible public HTTPS/WSS launch scripts and documentation for raw-IP short-lived certificates, sslip.io fallback, NAT-PMP renewal, router forwarding, and TURN configuration.
- Ship readable demo and oracle sources with the npm package and refresh generated package output.
