# 2.21.1

- Document chunks-first snapshot selection for spread-based consumer test doubles:
  hide `chunks`, disable `chunkedKeyframe` per follower/sync, or disable producer
  `replay.chunks` when intercepting `keyframe()`.
- Explain the rolling ten-release change history and how to retrieve older notes
  from their published npm archives. Confirm 2.13.0 was a five-fix release, not a
  technical-only version.
- Documentation-only patch; no runtime behavior, public types or dependency changes.
