# 2.16.1

- Fix scaffold client failover across independent role projections: switch replay with
  `reset: true, since: -1`, preserving the existing client Store and refreshing its data.
- Bound endpoint readiness to 5 seconds, exclude failed serving candidates and fall back to
  authority even while dead nodes remain in the roster; close pending hubs and readiness timers.
- Regenerate pizzeria, rental, apartments and small-jobs clients without changing public library
  interfaces or replay internals.
- Add the shipped pizzeria `test:network` process check for one/all node crashes, retained mirrors,
  authority archive restart and original command receipts; extend socket lifecycle checks for
  silent endpoints and close during handshake.
- Document same-line cursor handoff versus independent projection reset. Applications adopting
  the refreshed generated client can remove their duplicate reset, endpoint deadline and failed
  candidate workarounds; retain their domain/UI adapter and network acceptance checks.
