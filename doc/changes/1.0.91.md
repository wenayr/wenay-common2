# 1.0.91

- Add the durability pipeline: `firstSeq` on the replay line (numbering survives process lifetimes), `Observe.createDurableStoreReplay` (state + seq hydrate from `ReplayStorage`; a mirror reconnecting with its old seq gets the exact persisted tail, no keyframe reset; new patches + cadence keyframes archive back) and `openFsReplayStorage` in `wenay-common2/server` (JSONL append-log with atomic `compact()`).
- Add the flight recorder: `Replay.createJsonlReplayWriter` / `loadJsonlReplay` plus `Observe.playbackStoreReplay` — record any patch line, play it back as an ORDINARY head at any pace (`speed`, `Infinity` = instant); `storeReplayAt` stays the random-access companion.
- Close the failover tails: `createArtifactHost({store, storage: {adoptKey}})` adopts a promoted mirror's catalog (the artifact-N id line continues, private keys recover from the content-hash byte cache); `createNodeIdMinter` lifts per-node id namespaces from a demo convention into a primitive.
- Add `Observe.createNodeHealth` — stats() of local primitives aggregated into one mirrorable store — and an additive line descriptor: `exposeStoreReplay({describe})` → `remote.describe()` / `readReplayDescriptor`.
- Oracles 67 → 73: durable restart over a real socket, seeded chaos (edge bounces + abrupt leader kill + promote, convergence/seq/epoch invariants), artifact adopt, id minter, node health, record/playback.
- Repo hygiene: dead `srcClient`/`srcServer` and stray files removed, the Cyrillic-letter filename fixed, `LICENSE` added, README gained a Quick start, and ALL code comments are now English (runtime strings deliberately untouched).
