# 1.0.80

- Add `Artifact.createArtifactHost/client` and `createArtifactFrame`: owner-filtered Store/replay descriptors, private storage keys, short-lived authorized open instructions, expiry/revoke lifecycle, and origin-pinned sandboxed iframe mounting.
- Extend the AI demo with an artifact storage adapter and isolated `artifact.localhost` counter app; add the real Socket.IO/RPC artifact oracle and the full storage/retention/security contract.
