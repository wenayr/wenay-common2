# 1.0.77

- Add `Resource.createFileJobHost` / `createFileJobClient`: injected storage upload/download intents and cancellable AI jobs, with owner-filtered Store/replay state over the existing RPC connection.
- Add the real Socket.IO resource oracle and a visible demo flow for upload → confirm → AI progress/result → download; bytes stay in the storage port, never in Store/replay.
