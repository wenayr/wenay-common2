# 1.0.78

- Add `Ai.createAiRunHost` / `createAiRunClient`: provider-neutral, account-filtered AI runs with replayed state/events, idempotent `requestId`, approvals, follow-up input, artifact descriptors and cancellation/late-output guards.
- Add the real Socket.IO AI-run oracle, a visible demo assistant flow, and `doc/AI-RUN-PROTOCOL.md` describing the provider, resource and security boundaries.
