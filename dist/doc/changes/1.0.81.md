# 1.0.81

- Add `Conversation.createConversationHost/client`: participant-filtered logical channels, immutable
  versioned blocks, scoped revisioned facts, semantic replay and atomic persistence receipts on the
  existing RPC connection.
- Extend the demo with a two-participant root → child-dialogue → fact stand; add the real Socket.IO/RPC
  Conversation oracle and the complete ownership, retention, AI/Artifact bridge contract.
