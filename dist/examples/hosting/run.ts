// Generated by scripts/generate-examples.mjs; edit the source in the repository.
import {createHosting} from './service'

async function main() {
    const hosting = await createHosting({tenants: ['my-site'], port: Number(process.env.PORT ?? 0)})
    try {
        const first = await hosting.control.deploy('my-site', 'v1')
        console.log('Your local site:', first.endpoint)
        await hosting.control.deploy('my-site', 'v2')
        console.log('Updated to v2 at the same address. Press Ctrl+C to stop.')
        process.once('SIGINT', function stop() { hosting.close().catch(fatal) })
        process.once('SIGTERM', function stop() { hosting.close().catch(fatal) })
    } catch (error) { await hosting.close(); throw error }
}

function fatal(error: unknown) { console.error(error); process.exitCode = 1 }
main().catch(fatal)
