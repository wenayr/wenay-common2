// Generated by scripts/generate-examples.mjs; edit the source in the repository.
import express from 'express'
import {randomBytes} from 'node:crypto'
import type {Ai} from 'wenay-common2'
import {listen} from 'wenay-common2/listen'
import {createRpcServerAuto} from 'wenay-common2/rpc'
import {createTokenCodec} from 'wenay-common2/server/auth'
import {createHttpFacadeServer} from 'wenay-common2/server/http'
import {createSupportService, DEMO_ACCOUNTS, type tAccount} from './service'
import {page} from './page'
import {createHostResource} from './http-host'

export async function startSupportHost(deps: {port?: number, stepMs?: number, runner?: Ai.AiRunRunner} = {}) {
    const transport = createHostResource({host: '127.0.0.1', port: deps.port ?? 0, socket: {transports: ['websocket']}})
    const {app, io} = transport.resource
    let closeService = function noService() {}
    let closing: Promise<void> | undefined
    function close() {
        closing ??= Promise.resolve().then(async function closeResources() {
            // Send a namespace disconnect before RPC cleanup can end a live replay line.
            try { io.disconnectSockets(true) }
            finally {
                const stopped = transport.close()
                try { closeService() }
                finally { await stopped }
            }
        })
        return closing
    }
    try {
        const service = createSupportService(deps)
        closeService = service.close
        const codec = createTokenCodec({secret: randomBytes(32).toString('hex'), ttlMs: 60_000})
        app.use(express.json({limit: '32kb'}))
        app.get('/', function supportPage(_request, response) { response.type('html').send(page) })
        app.post('/demo-session', function demoSession(request, response) {
            const account = request.body?.account
            if (!DEMO_ACCOUNTS.includes(account)) {
                response.status(400).json({error: 'unknown demo account'})
                return
            }
            response.json({token: codec.issue({sub: account})})
        })
        for (const account of DEMO_ACCOUNTS) {
            const fragment = service.source.principal(account)
            const gate: express.RequestHandler = function authorize(request, response, next) {
                const verdict = codec.verify(request.headers.authorization?.replace(/^Bearer /, ''))
                if (!verdict.ok || verdict.claims.sub != account) {
                    response.status(401).json({ok: false})
                    return
                }
                next()
            }
            createHttpFacadeServer({app, basePath: '/api/' + account, method: 'get', middleware: gate,
                object: {runs: () => service.view.runs(account)}})
            createHttpFacadeServer({app, basePath: '/api/' + account, method: 'post', middleware: gate,
                object: {createRun: fragment.createRun, cancelRun: fragment.cancelRun}})
        }
        io.on('connection', function connected(socket) {
            const [gone, disconnected] = listen<[]>()
            socket.on('disconnect', function disconnect() {
                gone()
                disconnected.close()
            })
            createRpcServerAuto({socket, socketKey: 'support', object: {}, disconnectListen: disconnected,
                auth: {gate: true, resolveAuth(token: unknown) {
                    const verdict = codec.verify(token)
                    if (!verdict.ok) throw new Error(verdict.reason)
                    return {object: service.source.principal(verdict.claims.sub), ack: {ok: true, account: verdict.claims.sub},
                        expiresAt: verdict.claims.exp, renewBeforeMs: 10_000}
                }},
            })
        })
        await transport.control.listen()
        return {url: transport.view.url(),
            source: {token(account: tAccount) { return codec.issue({sub: account}) }}, close}
    } catch (error) {
        await close().catch(function ignoreCleanupFailure() {})
        throw error
    }
}
export type SupportHost = Awaited<ReturnType<typeof startSupportHost>>
