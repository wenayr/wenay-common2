{
    "name": "{{name}}",
    "private": true,
    "version": "0.1.0",
    "//": "Incubator note: while this template lives inside the wenay-common2 repo, the .ts files import from '../../../src/...'; after graduation they import the 'wenay-common2' package below instead (each file carries a TODO(graduation) marker at its import block).",
    "scripts": {
        "node": "tsx node.ts",
        "typecheck": "tsc --noEmit"
    },
    "engines": {
        "node": ">=20.0.0"
    },
    "dependencies": {
        "express": "^5.2.1",
        "socket.io": "^4.8.3",
        "socket.io-client": "^4.8.3",
        "wenay-common2": "^2.15.0"
    },
    "devDependencies": {
        "@types/express": "^5.0.6",
        "@types/node": "^26.1.2",
        "tsx": "^4.23.1",
        "typescript": "^7.0.2"
    }
}
